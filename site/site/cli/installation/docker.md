# Docker

Each version of olpat is also available packaged in a small Docker image on the [GitHub Container Registry](https://github.com/weichweich/olpat/pkgs/container/olpat).

```sh
docker run ghcr.io/weichweich/olpat --help
```
