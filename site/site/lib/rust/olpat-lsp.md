# olpat LSP

The [`olpat-lsp`](https://crates.io/crates/olpat-lsp) crate exposes a language server implementation that uses [`olpat-lsp-async-stub`](https://crates.io/crates/olpat-lsp-async-stub), it is generic over its environment so it is possible to embed it in your own software if needed.
