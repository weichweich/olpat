# olpat Common

The [`olpat-common`](https://crates.io/crates/olpat-common) includes functionality for schema-based validation and other commonly used tools that are shared between the CLI and the LSP.

::: warning

The `olpat-common` crate is not intended for general use as-is and it's only here for the sake of completeness.

:::

